# 如何判断代码中使用了哪种线程创建方式？

## 🔍 判断方法

### 方法1：继承 Thread 类

**特征：**
```java
class SomeClass extends Thread {
    @Override
    public void run() {
        // 任务逻辑
    }
}

// 使用方式
SomeClass thread = new SomeClass();
thread.start();
```

**识别要点：**
- ✅ 类直接继承 `Thread`
- ✅ 重写 `run()` 方法
- ✅ 直接调用 `start()` 方法

---

### 方法2：实现 Runnable 接口

**特征：**
```java
class SomeTask implements Runnable {
    @Override
    public void run() {
        // 任务逻辑
    }
}

// 使用方式1：传给 Thread
Runnable task = new SomeTask();
Thread thread = new Thread(task);
thread.start();

// 使用方式2：在线程池中使用
ExecutorService executor = Executors.newFixedThreadPool(5);
executor.execute(task);

// 使用方式3：Lambda 表达式
new Thread(() -> {
    // 任务逻辑
}).start();
```

**识别要点：**
- ✅ 类实现 `Runnable` 接口
- ✅ 重写 `run()` 方法
- ✅ 需要传给 `Thread` 构造函数或线程池的 `execute()` 方法
- ✅ 常见使用 Lambda 表达式：`() -> { ... }`

---

### 方法3：实现 Callable 接口

**特征：**
```java
class SomeTask implements Callable<String> {
    @Override
    public String call() throws Exception {
        // 任务逻辑
        return "返回值";
    }
}

// 使用方式1：配合 FutureTask
Callable<String> task = new SomeTask();
FutureTask<String> futureTask = new FutureTask<>(task);
Thread thread = new Thread(futureTask);
thread.start();
String result = futureTask.get();

// 使用方式2：在线程池中使用
ExecutorService executor = Executors.newFixedThreadPool(5);
Future<String> future = executor.submit(task);
String result = future.get();
```

**识别要点：**
- ✅ 类实现 `Callable<T>` 接口
- ✅ 重写 `call()` 方法（注意是 `call()` 不是 `run()`）
- ✅ 方法有返回值
- ✅ 方法可以抛出异常
- ✅ 通常配合 `FutureTask` 或线程池的 `submit()` 方法使用

---

## 📋 快速识别表

| 特征 | 继承 Thread | 实现 Runnable | 实现 Callable |
|------|------------|--------------|--------------|
| **接口/类** | `extends Thread` | `implements Runnable` | `implements Callable<T>` |
| **方法名** | `run()` | `run()` | `call()` |
| **返回值** | `void` | `void` | `T`（有返回值） |
| **异常** | 不能抛出 | 不能抛出 | 可以抛出 |
| **使用方式** | `new MyThread().start()` | `new Thread(task).start()` | `new FutureTask<>(task)` |
| **线程池** | 不推荐 | `executor.execute(task)` | `executor.submit(task)` |

---

## 🔎 实际代码分析示例

### 示例1：继承 Thread

```java
class WorkerThread extends Thread {
    @Override
    public void run() {
        System.out.println("工作");
    }
}

WorkerThread worker = new WorkerThread();
worker.start();
```

**判断：** ✅ 继承 Thread 类

---

### 示例2：实现 Runnable

```java
class WorkerTask implements Runnable {
    @Override
    public void run() {
        System.out.println("工作");
    }
}

Thread thread = new Thread(new WorkerTask());
thread.start();
```

**判断：** ✅ 实现 Runnable 接口

---

### 示例3：Lambda 表达式（Runnable）

```java
new Thread(() -> {
    System.out.println("工作");
}).start();
```

**判断：** ✅ 实现 Runnable 接口（Lambda 简化写法）

---

### 示例4：实现 Callable

```java
class WorkerTask implements Callable<String> {
    @Override
    public String call() throws Exception {
        return "结果";
    }
}

FutureTask<String> futureTask = new FutureTask<>(new WorkerTask());
new Thread(futureTask).start();
String result = futureTask.get();
```

**判断：** ✅ 实现 Callable 接口

---

### 示例5：线程池中使用

```java
ExecutorService executor = Executors.newFixedThreadPool(5);

// 方式1：execute(Runnable)
executor.execute(() -> {
    System.out.println("任务");
});

// 方式2：submit(Callable)
Future<String> future = executor.submit(() -> {
    return "结果";
});
```

**判断：**
- `execute()` → ✅ Runnable
- `submit()` → ✅ Callable（有返回值）

---

## 🎯 分析 CornerStone 代码的方法

如果你有 CornerStone 的代码，可以按以下步骤分析：

### 步骤1：查找线程创建代码

搜索关键词：
- `extends Thread`
- `implements Runnable`
- `implements Callable`
- `new Thread(`
- `executor.execute(`
- `executor.submit(`

### 步骤2：查看类定义

```java
// 如果是这样：
class SomeClass extends Thread {
    // → 继承 Thread
}

// 如果是这样：
class SomeClass implements Runnable {
    // → 实现 Runnable
}

// 如果是这样：
class SomeClass implements Callable<String> {
    // → 实现 Callable
}
```

### 步骤3：查看使用方式

```java
// 如果直接 start()
someObject.start();
// → 继承 Thread

// 如果传给 Thread 构造函数
new Thread(someObject).start();
// → 实现 Runnable

// 如果配合 FutureTask
FutureTask<String> futureTask = new FutureTask<>(someObject);
// → 实现 Callable
```

---

## 💡 常见模式识别

### 模式1：匿名内部类

```java
new Thread(new Runnable() {
    @Override
    public void run() {
        // 任务逻辑
    }
}).start();
```
**判断：** ✅ 实现 Runnable 接口

---

### 模式2：Lambda 表达式

```java
new Thread(() -> {
    // 任务逻辑
}).start();
```
**判断：** ✅ 实现 Runnable 接口（Lambda 简化）

---

### 模式3：方法引用

```java
Runnable task = this::doWork;
new Thread(task).start();
```
**判断：** ✅ 实现 Runnable 接口

---

## 📝 总结

**快速判断口诀：**

1. **看到 `extends Thread`** → 继承 Thread
2. **看到 `implements Runnable`** → 实现 Runnable
3. **看到 `implements Callable`** → 实现 Callable
4. **看到 `new Thread(...)`** → 通常是 Runnable
5. **看到 `FutureTask`** → 通常是 Callable
6. **看到 `executor.execute()`** → Runnable
7. **看到 `executor.submit()`** → Callable（有返回值）

**如果你能提供 CornerStone 的具体代码片段，我可以帮你准确判断！**

